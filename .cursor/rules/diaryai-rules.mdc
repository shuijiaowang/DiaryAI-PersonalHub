---
description: DiaryAI-PersonalHub project rules - hard constraints for AI when modifying this codebase
alwaysApply: true
---

# DiaryAI-PersonalHub · 工程硬约束（来自设计文档 §8）

任何对本仓库的代码改动都必须严格遵守以下规则。违反任意一条都视为缺陷。

## LLM / AI

1. **任何 LLM 调用必须经过 `server/app/ai/providers/base.py` 的 `LLMProvider` 抽象。**
   禁止在业务代码（services / api / modules / tasks）里直接 `import openai` 或自行 HTTP 调用模型。
   新增模型 = 新增一个 `LLMProvider` 子类 + 在 `providers/registry.py` 注册。

2. **任何 AI 输出必须经过 Pydantic 校验后才能进数据库。**
   入口已在 `app/ai/pipelines/diary_parser.py`：解析失败必须重试（默认 3 次）并把上轮错误回灌给模型，而不是直接吞错。

3. **每次 LLM 调用必须落审计表 `ai_call_log`。** 走 `app/ai/audit.py`。

4. **同 prompt + 同 model 的结果必须可缓存**（`app/ai/cache.py`）。新增 prompt 流程时遵循同一缓存模式。

## 模块（business modules）

5. **加新模块 = 在 `server/app/modules/<code>/` 下新增子类 + `@register`。**
   不允许在 `action_executor.py / diary_parser.py` 等主流程里写 `if module_code == "xxx"` 的硬编码分支。

6. 每个模块必须提供：`code / name / DataSchema / prompt_fragment / actions`，可选实现 `apply_action`（仅当 `actions` 非空）。

## 数据库 / 服务分层

7. **API 路由（`api/v1/*`）只做入参校验和调用 Service。** 不允许在路由里直接写 SQL 或操作 ORM 对象。

8. **数据库写操作（add/commit/delete/flush）必须在 Service 层。** Repository 层只负责数据访问，不写业务逻辑。

9. 新加的表必须经 Alembic 迁移生成；不允许手写 DDL 或在运行时 `Base.metadata.create_all`。

10. 时间字段统一 `DateTime(timezone=True)`，业务里全程 UTC，前端展示时再转本地时区。

## 配置 / 安全

11. **API Key、密码、密钥**只能从环境变量读，必须经过 `app/core/config.py:Settings`。
    禁止源码里出现任何明文 secret，禁止把 `.env` 提交进 git。

12. 路由必须使用 `app.api.deps.get_current_user` 进行鉴权，所有数据查询必须按 `user_id` 隔离。

## 测试

13. 加新模块至少补一条最小用例到 `server/tests/test_modules.py`（或对应模块自己的测试文件）。
14. 涉及 LLM 的单元测试必须 mock 掉 `LLMProvider`，禁止跑真 API（除非显式放在 `tests/e2e_with_real_llm/`）。

## 文档与 AI 工作流

AI 的长期记忆分两处：`docs/ai/`（给 AI 读，结构化、短、token 友好）与 `docs/human/`（给开发者读，通俗）。硬约束本身（即本文件）始终加载，不需要额外读。

15. **开工前先对齐上下文**：
    - 任何任务开始前必读 `docs/ai/07_进度与状态.md`（知道现在到哪了、有什么坑）。
    - 按 `docs/ai/README.md` 的"按任务选读"表，只读本次任务需要的文件；不要无差别通读 `docs/ai/`。

16. **阶段性任务完成后必须同步文档**（未同步 = 任务未完成）：
    - **加/改业务模块** → `docs/ai/04_模块约定.md` 的模块表 + `docs/ai/07_进度与状态.md`
    - **加/改数据库表** → `docs/ai/03_数据模型.md` + `docs/human/数据库一览.md` + Alembic 迁移
    - **改 AI pipeline / 主流程** → `docs/ai/05_核心流程.md`；若是结构性变化还要写 `docs/ai/08_决策记录.md`
    - **完成 MVP 清单任一项** → `docs/ai/07_进度与状态.md` 勾选 + `docs/human/开发进度.md` 追加里程碑
    - **修复已记录的 bug** → `docs/ai/07_进度与状态.md` 的"已知 bug"区移除
    - **做出重大技术决策 / 推翻旧方案** → 先写 `docs/ai/08_决策记录.md` 新增 ADR，再改代码
    - **不允许动** `docs/human/测试体验与反馈.md` 和 `docs/human/下一步建议.md` 的"随手想法"区（除非用户明确要求）

17. **文档位置与体量约束**：
    - 所有架构 / 设计 / 进度 / 决策类文档只能放 `docs/ai/` 或 `docs/human/`，不允许在 `server/` / `web/` / 仓库根下散落 `README_架构.md` `DESIGN.md` 之类的文件（各子项目的 `README.md` 仅写"如何安装启动"是允许的）。
    - `docs/ai/` 下单文件目标 ≤ 200 行；超了就拆，或把论述性内容搬去 `docs/human/`。
    - 同一事实只在一处维护，其他地方链接过去，禁止冗余复制。
