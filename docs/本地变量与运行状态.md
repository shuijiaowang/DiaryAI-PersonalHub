python已安装，版本3.13.4
node已安装，版本v23.8.0
nmp已安装，版本10.9.2
pgsql,已安装，版本17.9，密码123456，已创建数据库diary

deepseek:
    key:DiaryAI
    密钥：sk-214b428636d24353b733d8220210788e

 
python -m venv .venv √
.\.venv\Scripts\Activate.ps1 √
pip install -e ".[dev]" √
alembic revision --autogenerate -m "init" //失败
alembic upgrade head

pip install email-validator
uvicorn app.main:app --reload --port 8000

成功运行后端。
成功运行前端
成功注册+登录
成功提交日记，
解析日记会先直接返回解析中，等待刷新后再次点击解析解析成功了
可以可消费统计记录。