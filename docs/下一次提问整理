//本文用于我自己使用，可按开发阶段持续调整并记录原因。
可以考虑用其他AI生成可直接复制给 Cursor 的提问模板，每此提问包括一组相关问题，具有相同的上下文，节省token的同时也不受多任务的过多影响。

2.关于本项目，我还是有一部分疑问的，一点是关于重复性，如消费模块记录价格，饮食模块也记录价格，好像是重复的但也是必要的？这里的重复好像无关紧要的？
3.我用Navict看数据库的主键id都是int8类型，这个int8是1-255还是八位的int？没搞错吧。
4.我打算先专注于几个模块的开发，之后据此再逐渐完善其他模块
- 如，消费，备忘录，打卡，饮食。
5. [已完成 2026-04-20]
我想要单独一个页面来展示消费统计。
日/周/月/年的数据统计
列表表格展示。
如一个日历，每天这样的展示。
→ 已新建「统计 / 消费统计」独立页，见 `docs/human/开发进度.md` 2026-04-20 条目。
→ "手动补录 vs 修改日记原文"问题的最终结论见 `docs/ai/08_决策记录.md` 的 ADR-007：
  原文永不被系统改写；手动补录走 event.locked=True，重解析不会被覆盖。
6.突然想到一个问题就是，如消费的评价，例如饮食的评价，好不好吃这种是归饮食模块管，但也是归消费模块管理啊？
目前看来评价可以单独写在饮食模块里，不用归消费管理，因为看起来重复了，但如果这个人只开启了消费模块的话，那就无法记录饮食模块的评论了。
但如果以后模块的数量越来越多，互相重复不是个好事吧，虽然也是独立的，但好像也会造成冲突，者之间的度我功力太浅，不知道改如何把握。
我一下子想到的就是模块之间相互引用，或者底层模块由我提供，用户自制模块根据底层模块二次加工得到？？？不清楚。
7.另一个问题就是关于修改性质。
例如之后的备忘录，提取了后给前端，前端点击完成后修改数据库已完成，但是日记里的原文还是未完成，这就冲突了，如果重新解析的话，岂不就又变成了未完成。
所以是否要修改原文呢？
8，关于全局参数用户画像，应该需要用户自己填写以及AI随着日记逐渐生成，不正确/缺失用户自己去填写修改？
这样也会产生其他问题，如他的地址，他搬家了的话地址也要修改？但旧地址好像也可以保留。还是覆盖呢，这个问题后面再说吧。



我的想法是把如消费记录这个给独立出来？或者说把某些内容独立出来，因为我可能想要进行二次开发，现在的解析不够我想要的？或者说我这种想法还是太复杂了。

换句话说，我是在想之后要深入拓展有关消费记录的部分，目前的设计可能不符合要求？
目前就是非常普通的消费记录，之后我想改为记账。
如从最基本的某天买了什么，某天发了工资。
拓展为我理想的双记账系统（普通记账+消费记账）
昨天交了房租水电/租的电动车/工资/一箱苹果/会员，平摊到每一天，
可以看这一天的真实消费（今日房租900/30+今日电动车租金100/30+会员费30/30+吃了一个苹果（一箱6个）12/6+吃了两个鸡蛋（一盒12个）6.5/12*2+电动牙刷（4月1买的）81/（今天-4.1的天数，动态的））
如果真要这么设计的化，可以单独写一个新项目了，但是现在又依赖于当前这个项目的逻辑。所以我现在感觉到了纠结，不知道如何设计是好
D. 早期日记修改后的级联影响
如果我修改第1天日记，可能影响第2~7天结果。请设计：

revision 与 stale 标记机制
何时触发“从某天起重算”
全量重算 vs 增量重算策略
前端如何提示“数据可能过期”
要求：先给 MVP 可做方案，再给进阶方案（快照/后台任务）。



双记账系统
现金流视图（普通记账）：钱哪天付出去/收进来，和常规记账一样。
权责视图（消费记账）：哪天真正消耗了价值，（平摊30天、按件数、按使用天数动态摊销）


DiaryAI 提问文档（原文策略 + 跨日关联 + 重算机制）
1. 背景与当前纠结
我在开发 DiaryAI-PersonalHub，目前核心纠结有四点：

提示词如何规范化（后续复杂度会持续上升），例如双记账系统模块。
跨日数据关联如何设计（例如昨天买鸡蛋，今天吃鸡蛋，后天扔鸡蛋，昨天做计划今天执行/放弃计划）
日记原文是否允许系统修改（用户可见性 vs 数据纯净性）
早期日记被修改后，后续多天结果如何处理（级联重算风险）
2. 我当前偏好（请按此优先设计）
我希望“用户可见且可控”：
AI 不要直接改原文原句
可以在末尾追加例如“系统建议补记”，由我手动确认/复制
希望有“看得见的修改结果”（不是只在数据库里黑箱变化）
允许调整早期 ADR（例如 ADR-007），不被旧方案锁死，现在仍处于设计开发阶段，没有什么是不能不修改的，可以适当去掉锁死的提示词，防止跑偏.

我感觉下面这三者都有其受众用户
原文完全只读， //保留证据。
仅允许“末尾追加区块”（AI 生成，用户确认后写入）， //作为开发者我是倾向于这个末尾追加
不直接修改原句子而是在句子后面插入修改
AI可直接改原文（如不推荐请说明风险）， 直接修改原句//但之后系统完善AI加强，这个也挺好的

但也要注意,如果用户修改后第二次解析的话呢，应防止重复记录之类的。

3.提示词会越来越复杂化怎么处理，
如消费记录，转为双记账系统，多人协作记账，等等会变得非常庞大？
如何限制 token 膨胀
如何尽量独立模块，转为可单独二开？

如何做失败重试与错误回灌
如何把“提取”和“关联”拆成两步，降低复杂度
C. 跨日关联（鸡蛋案例）
基于案例：

Day1：买一盒鸡蛋
Day2：煎鸡蛋1个
请给出：

事件模型（采购事件、消耗事件、关联关系）
解析时是否立即强关联，还是延迟关联
最小可行实现（MVP）与后续演进路径

---
1.修改文档，允许调整早期 ADR（例如 ADR-007），不被旧方案锁死，现在仍处于设计开发阶段，没有什么是不能不修改的，可以适当去掉锁死的提示词，防止跑偏.
2.设计允许“末尾追加区块”（AI 生成，用户确认后写入）， //作为开发者我是倾向于这个末尾追加，
等到后期让AI直接修改原文那也就是根据记录修改，也会很简单，

场景：应该就这些情况把暂时：
原文->解析->前端展示。
用户增删改原文->重新解析->能否增删改数据库数据->前端展示。
前端展示->用户图形化修改了数据->数据库变化->原文末尾追加修改记录(自然语言)
原文末尾追加修改记录(自然语言)->重新解析->这个结果应该不变。（问题1）
用户根据修改记录修改原文并删除修改记录->重新解析->这次解析应该是不变或是少量变化才对？

（问题1）：我不清楚解析是如何实现的，是一句一句的解析好像，还是统筹呢？肯定不能重复解析两次才对。而是追加修改合并为一句才对？不清楚现在系统是如何设计的，如果不对的话应该修改？
例如日记：
原文：今天早上吃的鸡蛋...下午去打乒乓球了，晚上吃的水煮蛋。
追加：早上吃的煎鸡蛋一个（补充煎，数量），下午去体育馆打乒乓球一小时（补充地点，时间），晚上吃的水煮蛋有两个，评价不好吃。（补充数量补充评价）

